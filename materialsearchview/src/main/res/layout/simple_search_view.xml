<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/searchRL"
    android:layout_width="match_parent"
    android:layout_height="?android:actionBarSize"
    android:background="@color/white"
    android:visibility="gone">

    <ImageButton
        android:id="@+id/backBT"
        style="@style/Button"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_centerVertical="true"
        android:paddingStart="@dimen/backButtonHorizontalPadding"
        android:paddingEnd="@dimen/backButtonHorizontalPadding"
        android:src="@drawable/ic_arrow_back"/>

    <EditText
        android:id="@+id/searchET"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_centerVertical="true"
        android:layout_toStartOf="@id/clearBT"
        android:layout_toEndOf="@id/backBT"
        android:background="@android:color/transparent"
        android:hint="@string/search_hint"
        android:imeOptions="actionSearch"
        android:inputType="textNoSuggestions"
        android:paddingStart="@dimen/backButtonHorizontalPadding"
        android:paddingEnd="@dimen/backButtonHorizontalPadding"
        android:textColor="@color/grey_900"
        android:textColorHint="@color/grey_600"
        android:textSize="@dimen/searchTextSize"/>

    <ImageButton
        android:id="@+id/clearBT"
        style="@style/Button"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_alignParentEnd="true"
        android:layout_centerVertical="true"
        android:paddingStart="@dimen/backButtonHorizontalPadding"
        android:paddingEnd="@dimen/backButtonHorizontalPadding"
        android:src="@drawable/ic_clear"/>

</RelativeLayout>